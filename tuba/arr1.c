#include<stdio.h>
int main(){
  
  int n, es=0, os=0, i;
  
  printf("Enter Element");
  scanf("%d",&n);
  
    int a[n]
    
      printf("Enter %d Element",n);
     
        for(i=0; i<n; i++)
        scanf("%d",&[i]);
        
          for(i=0; i<n; i++)
        {
            if(a[i]%2==0)
            es=es+a[i];
            else
            os=os+a[i];
          }
         
         printf("Even sum : %d", es);
        
         printf("Odd sum : %d", os);
return 0;}
